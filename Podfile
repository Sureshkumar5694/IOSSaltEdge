# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'SaltEdge' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  pod 'SaltEdge-iOS', '~> 2.6.0'
  pod 'SVProgressHUD'
  pod 'RealmSwift'
  pod 'Alamofire', '~> 4.3'
  pod 'DropDown'
  pod 'Charts'

  target 'SaltEdgeTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'SaltEdgeUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
